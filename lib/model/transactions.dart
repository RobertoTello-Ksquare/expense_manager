class Transactions {
  String title;
  String createdAt;
  double amount;
  String description;
  bool isIncome;

  Transactions({
    required this.title,
    required this.createdAt,
    required this. amount,
    required this.description,
    required this.isIncome,
  });
}
